#include "main.h"
#include <string.h>
/**
 * main - Entry point
 * _puts - function to use puts prototype
 * Return: Always 0 (Success)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);

}
